/**
 * 简单 AI —— 随机决策。
 * 依赖: ../../utils/shuffle
 */
exports.execute = function (gameState, aiOpenid) {
  // TODO: 实现随机策略
  // 1. 摸牌
  // 2. 随机插入位置
  // 3. 随机选对手 + 位置 + value 猜测（颜色由牌背可见）
  // 4. 猜 1 次后 pass
  return [];
};
